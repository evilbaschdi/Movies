<controls:MetroWindow x:Class="Movie.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="Movies" Width="1000" Height="700" Icon="b.ico"
                      Style="{DynamicResource VSWindowStyleKey}">
    <Grid>
        <!--DataGrid-->
        <DataGrid Name="MovieGrid" IsReadOnly="True" CanUserAddRows="False" SelectionUnit="FullRow" ColumnWidth="*" AutoGeneratingColumn="OnAutoGeneratingColumn"
                  GridLinesVisibility="Horizontal" MouseDoubleClick="MovieGridOnMouseDoubleClick" SelectionChanged="MovieGridSelectionChanged" CanUserDeleteRows="False" Margin="10,60,10,10">
            <DataGrid.ContextMenu>
                <ContextMenu Name="MovieContextMenu" StaysOpen="True">
                    <MenuItem Name="EditMenuItem" Header="Edit" Click="EditClick" />
                    <MenuItem Name="DeleteMenuItem" Header="Delete" Click="DeleteClick" />
                    <MenuItem Name="DistributeMenuItem" Header="Distribute that movie" Click="DistributeClick" IsEnabled="True" />
                    <MenuItem Name="GotBackMenuItem" Header="Got back" Click="GotBackClick" />
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <!--Button-->
        <Button Name="New" Click="NewClick" Content="Add new movie" Height="20" Width="155" Margin="0,10,10,0" VerticalAlignment="Top" HorizontalAlignment="Right" />
        <Button Name="Export" Click="ExportClick" Content="Export to Excel" Height="20" Width="155" Margin="0,35,10,0" VerticalAlignment="Top" HorizontalAlignment="Right" IsEnabled="False" />

        <!--Search-->
        <TextBox Name="SearchFilter" Height="20" Margin="10,10,0,0" TextWrapping="Wrap" TextChanged="SearchOnTextChanged" VerticalAlignment="Top" Width="400" HorizontalAlignment="Left" />
        <Button Name="SearchReset" Click="SearchResetClick" Content="Reset" Height="20" Width="50" Margin="260,10,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" />
        <ComboBox Name="SearchCategory" Height="20" Margin="310,10,0,0" VerticalAlignment="Top" Width="100" HorizontalAlignment="Left">
            <ComboBoxItem Content="Name" />
            <ComboBoxItem Content="Year" />
            <ComboBoxItem Content="Format" />
        </ComboBox>

        <!--<controls:Flyout Name="SettingFlyout" Background="Black" Position="Right" Margin="400,0,0,0">
        <Grid Name="Setting" />
        </controls:Flyout>-->
    </Grid>
</controls:MetroWindow>